
// (x + 3) * 10

const sumNumbers = function (a, b){
    return a + b
} 

const multiplyByTen = function(c){
    return c * 10
} 

const x = 4

const result = multiplyByTen(sumNumbers(x, 3))

console.log(result)